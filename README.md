# WTBlackListsData

WTBlackList 数据存储仓库(仅数据, 不含应用代码)。

- `blacklist.json` — 审核通过的黑名单
- `review_pending.json` — 待审核请求队列
- `nickname.json` — 共享昵称表

由服务端 Actions(review-sync / nickname-sync)定时维护。
**数据公开透明**: 所有数据公开可读, 上传即视为同意公开。
